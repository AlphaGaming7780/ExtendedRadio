import { getModule } from "cs2/modding"

const path$ = "game-ui/editor/data-binding/editor-tool-bindings.ts"

const editorToolsModule = getModule(path$, "tools");

export const Loc = getModule(path$, "tools");